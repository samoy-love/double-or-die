/**
 * Тип отладочного интерфейса для сквозных тестов и съёмки экранов.
 *
 * Объявление здесь ОДНО и берётся из клиента, а не переписывается копией.
 * Копия тут была, и она отстала ровно так, как отстают копии: метод появлялся
 * в `packages/client/src/debug.ts`, а сюда не доезжал, и тесты переставали
 * собираться — либо, хуже, съёмка экранов молча падала на «нет такой ручки».
 *
 * Импорт только типовой: `debug.ts` тянет за собой игровой цикл, а тестам
 * нужен лишь контракт. В рантайм такой импорт не попадает.
 */

import type { DebugApi } from '../packages/client/src/debug';

export type { DebugApi };

declare global {
  interface Window {
    __DOD__?: DebugApi;
  }
}
