/**
 * Общий слой: версии протокола, типы событий, всё, что знают сразу двое.
 *
 * Пакет объявлен структурой репозитория с 0.1.0 (TECH §10) и до сих пор
 * пустовал — а версии протокола при этом не существовало вовсе, хотя на неё
 * ссылаются и формат реплея, и рукопожатие лобби. Пустой пакет в списке
 * структуры хуже отсутствующего: он выглядит сделанным.
 *
 * Правило зависимостей: `sim` не импортирует ОТСЮДА ТОЖЕ. Ядро не знает про
 * протокол и версии — оно про арифметику, и это то самое свойство, ради
 * которого куплены реплеи, античит и порт на консоли. Здесь живут вещи,
 * общие для клиента, инструментов и будущего сервера.
 */

export * from './protocol';
export * from './events';
